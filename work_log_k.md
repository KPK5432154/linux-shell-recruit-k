# 这是关于做本题时的记录

## 2026.9.13  
关于指令
```
第一次进入仓库后，先执行：

chmod +x check.sh tools/check-project scripts/start-workers.sh scripts/worker.sh  
```  
这是一个改变文件权限的命令
```
chmod ->change mode  //改变文件能被使用的权限
+ x  ->execute  //加上可运行权限  
ps: 
+ r ->read  //读取
+ w ->write  //写  

check.sh 
tools/check-project 
scripts/start-workers.sh 
scripts/worker.sh
  //给这四个文件改变权限
```
关于  
```  
ls
```  
这是一个用于查看文件夹目录的指令
```  
ls ->list //打印列表
``` 
关于 cat
```  
cat ->concatenate //连接 用于查看文件内容，打印到终端
``` 



