# Task 02 Answer

请在这里回答题目中的两个问题。
问题一：因为初始是在这个仓库的文件夹内（即linux-shell-recruit-k），输入文件名，默认只会在PATH下查找。如果需要执行当前仓库中的某个命令程序，我们要不然添加到临时或者永久PATH中，要不然使用相对路径或者绝对路径来实现，这里./tools/recruit-info就是相对路径
问题二：~~我们输入ls，系统会去PATH中寻找这个命令，找到后才运行它，最后输出我们看到的文件夹内容~~  
我们输入ls。shell会先检查是否是别名（ 对某个命令赋予的其他名字）然后再检查是不是shell内置的命令，最后才去PATH中找是不是其中的某个命令程序

